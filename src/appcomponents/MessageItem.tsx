import * as React from "react";

const messageItem = (props) => {
  return <div>TODO Message Item</div>; //make message item that , amoung other things, updates itself as stale when message gets old, 
  //also should use fb style "3 minuts ago time indicators"
}

export default messageItem;